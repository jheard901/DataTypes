#pragma once

//a slew of useful commands/shortcuts to make use of

void DP(const char chs[]);	//DebugPrintString shorthand for windows
int GetInt(int min, int max);	//used for quickly getting a valid int between a min and max
